version: '3'
services:
  onlinemat:
    build:
      context: ./
    ports:
      - "3000:3000"
    container_name: frontend
    stdin_open: true

    env_file:
      - .env

    volumes:
       - ./:/usr/src/app
       - /usr/src/app/node_modules