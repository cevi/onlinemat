version: '3'
services:
  onlinemat-frontend:
    build:
      context: ./
    ports:
      - "3000:3000"
    container_name: onlinemat-frontend
    stdin_open: true
    volumes:
       - ./:/usr/src/app
       - /usr/src/app/node_modules